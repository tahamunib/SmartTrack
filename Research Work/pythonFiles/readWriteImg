import numpy as ny
import cv2

img = cv2.imread("car.jpg",0)
cv2.imshow("Image",img)
k = cv2.waitKey(0)
if k == 27:
    cv2.destroyAllWindows()
elif k==ord('s'):
    cv2.imwrite('cargr.png',img)
    cv2.destroyAllWindows()
    
